# A11Y — 无障碍与语义

> **feature 文档 · 规范类 + 功能开发**。
> **管理 scope**：页面语义结构（`h1` / `section` / `aria-labelledby`）、颜色对比、图片 alt 来源、键盘可达路径、图标 `aria-hidden` 约定、Storybook a11y addon 的零违规策略。改任何 aria、焦点顺序、对比度先改这里。
> 归属：intj（索引）+ feature（细节） · 最后更新：2026-09-17
> 来源：2026-09-17 从 [PROJECT.md](../PROJECT.md) §5.7「无障碍与语义」 迁出。PROJECT.md 只保留 §3.2 索引与 §3.1 口供；本文与口供冲突时先改口供。

---

## 一、目标

甲方没有单列无障碍条目，但 §4D 的 focus 态、§4E 的逐态走查和 Storybook a11y addon（`test: "error"`，STORYBOOK.md §三）让它成为硬门禁：一处违规，story 就红。

## 二、无障碍与语义规范

- 一页一个 `h1`，区块用 `section` + `aria-labelledby`。
- 颜色对比 AA；图片 alt 来自数据。
- 键盘走通全页：导航、抽屉、手风琴、轮播、BMI 表单。

## 三、补充约定

- 可交互元素必须是原生可聚焦元素，禁止 `div onClick`（COMPONENTS.md §三 第 5 条）
- 图标一律 `aria-hidden`，含义由旁边文本或 `aria-label` 承担（COMPONENTS.md §三 第 6 条）；证言卡社交图标按 PROJECT §11 #1 为纯装饰
- focus-visible 样式：全站统一 2px 强调色外环 + 2px offset（MOTION.md §二 状态规则 2）
- 移动抽屉打开时焦点进入抽屉、Esc 关闭、关闭后焦点回到触发按钮

## 四、待实现 / 已知问题

- 对比度 AA 需在 token 定稿后逐色对验（TOKENS ST-2 之后）
- P5 全页键盘路径已验证：skip、菜单循环/退出/目标聚焦、字段与单位、FAQ、轮播、Footer。

## 实现计划

进度：2 / 2 subtasks 完成（100%）

- [x] ST-1: 原语组件阶段每个 story 过 a11y addon 零违规（随 COMPONENTS ST-2）
- [x] ST-2: 区块完成后键盘全页走查 + 对比度对验，结果记入 §测试记录

## 测试记录

- 2026-09-17 P4：79 原语 stories × 2 视口共 158 次 axe 检查为零违规，未 disable 规则。18 个 Playwright 测试覆盖 disabled / 真实链接、Tab / Space / Enter / radio 方向键、数字字段描述与无效态、details、轮播边界 / 屏外 inert、跑马灯暂停 / OS reduce。禁用 NumberField 只淡化 input，单位仍 AA 可读；空 Carousel track 明确 group 角色且不加入 Tab。
- 跑马灯 Pause / Resume 使用随动作改名的普通按钮，不混用 `aria-pressed`，依据 [W3C Button Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/button/)。Chip 的 toggle 名称保持不变并使用 aria-pressed。
- axe 与浏览器可访问树检查不等于读屏软件实听；VoiceOver / NVDA 与全页键盘走查仍待 P5/P7。

- 2026-09-17 P5：页面 375/1440 默认、BMI 展开与 reduced-motion axe 全规则通过；修正嵌套 OnlineCare 同名 region，轮播使用独立的 Online care benefits 标签。装饰职责序号 01/02 使用 aria-hidden SVG，实际角色标题独立可读。Hero benefit 采用 #00774d，白底 5.61:1；正常文本与控件 axe 对比检查通过。原生 dialog 循环 Tab、Escape、返回触发器、锚点转移焦点、跨 1024 关闭与恢复 body 滚动均通过。BMI 错误焦点/关联说明、计算结果 polite 通知、折叠保留状态验证通过。浏览器测试不替代 VoiceOver/NVDA 实听，后者未测。
